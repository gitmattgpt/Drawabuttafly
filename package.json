{
  "name": "drawabuttafly",
  "version": "0.1.0",
  "private": true,
  "description": "Camera-based butterfly creator and AR-style swarm prototype",
  "scripts": {
    "dev": "python3 tools/dev-server.py",
    "start": "npm run dev",
    "check": "node --check src/main.js && python3 -m compileall -q tools",
    "serve": "python3 -m http.server 8000"
  }
}
